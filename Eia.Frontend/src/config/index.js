export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5090';

export const PAGINATION = {
  DEFAULT_LIMIT: 15,
};

export const ROUTES = {
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
};